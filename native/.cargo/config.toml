# Linux ARM64 交叉编译配置 (在 x86_64 主机上编译)
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

# Windows MSVC 在 Linux/macOS 上交叉编译需要 xwin (可选，通常建议在 Windows 上编译 Windows 版)
# [target.x86_64-pc-windows-msvc]
# linker = "xwin"